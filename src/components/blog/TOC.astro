---
import type { MarkdownHeading } from "astro";
import { generateToc } from "@/utils/generateToc";
import TOCHeading from "./TOCHeading.astro";

interface Props {
	headings: MarkdownHeading[];
}

const { headings } = Astro.props;

const toc = generateToc(headings);
---

<aside class="hidden lg:sticky lg:top-12 lg:order-2 lg:block lg:w-[220px] lg:shrink-0">
	<nav class="border-l border-zinc-200 pl-5 dark:border-zinc-700">
		<h2 class="mb-4 text-sm font-semibold text-zinc-700 dark:text-zinc-200">
			Contents
		</h2>

		<ol class="space-y-1">
			{toc.map((heading) => <TOCHeading heading={heading} />)}
		</ol>
	</nav>
</aside>